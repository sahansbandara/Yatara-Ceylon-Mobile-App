{
  "name": "yatara-ceylon-mobile-assignment",
  "version": "1.0.0",
  "private": true,
  "description": "React Native + Express + MongoDB full-stack mobile assignment for Yatara Ceylon.",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "backend:dev": "npm run dev --prefix backend",
    "backend:start": "npm start --prefix backend",
    "backend:seed": "npm run seed --prefix backend",
    "backend:check": "find backend -path 'backend/node_modules' -prune -o -name '*.js' -print | xargs node --check",
    "frontend:start": "npm start --prefix frontend",
    "frontend:android": "npm run android --prefix frontend",
    "frontend:ios": "npm run ios --prefix frontend",
    "frontend:lint": "npm run lint --prefix frontend",
    "frontend:typecheck": "npm run typecheck --prefix frontend",
    "check": "npm run backend:check && npm run frontend:typecheck && npm run frontend:lint"
  }
}
