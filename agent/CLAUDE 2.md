# Agent Notes

This project has moved from web completion to mobile conversion.

Follow `AGENTS.md`, `agent/MEMORY.md`, `agent/TODO.md`, and `agent/BRIEF.md`.

Do not remove the existing Next.js web application. Build the assignment in `backend/` and `mobile/Yatara-Ceylon/`.
