# Mobile Completion Plan

1. Keep repository scope clean: Express backend, Expo mobile app, docs, agent notes.
2. Verify backend syntax and mobile TypeScript/lint after each structural cleanup.
3. Deploy backend to Render or Railway with `MOBILE_DB_NAME=yatara-mobile`.
4. Connect Expo app with `EXPO_PUBLIC_API_URL=https://<hosted-api>/api`.
5. Capture final evidence: screenshots, hosted health check, MongoDB mobile database proof, member CRUD demos.
