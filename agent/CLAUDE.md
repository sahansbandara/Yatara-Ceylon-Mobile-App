# Agent Notes

This repo is scoped to the Yatara Ceylon mobile assignment.

Do:

- Work in `backend/` for Express API changes.
- Work in `mobile/Yatara-Ceylon/` for React Native changes.
- Work in `docs/` for report, viva, deployment, API, and team evidence.
- Preserve MongoDB safety checks.

Do not:

- Recreate removed website code.
- Build a WebView wrapper.
- Use the old website database.
- Run seed scripts without confirming the active database is `yatara-mobile`.
